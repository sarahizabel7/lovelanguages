/**
 * Re-exports love language UI metadata from quiz.ts.
 *
 * `loveLanguageMeta` provides labels and descriptions for the
 * 5 love languages, separate from i18n (which holds the full
 * translated strings for results and tips).
 */
export { loveLanguageMeta } from '../../quiz';
export type { LoveLanguage } from '../../quiz';
